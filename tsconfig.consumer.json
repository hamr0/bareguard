{
  "// note": "Type-checks a fixture that imports the package the way a real TypeScript consumer would (`from \"bareguard\"`), resolving through the package.json `exports.types` mapping to the .d.ts emitted by `npm run build:types`. Proves the SHIPPED types resolve and are correct — not just that the source self-checks. Run after build:types.",
  "compilerOptions": {
    "noEmit": true,
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "target": "ES2022",
    "lib": ["ES2022"],
    "types": ["node"],
    "strict": true,
    "skipLibCheck": true
  },
  "include": ["test/types/**/*.ts"]
}
