{
  "_comment": "SAMPLE / STARTER rwx file for the rwx-poc.mjs throwaway POC. Not shipped, not read by src/. Shape per docs/wiki/releases-roadmap.md lines 353-500 (rwx section) / bareguard-prd.md §23.",
  "_notes": "\"git log\": \"r\" was removed to match the shipped bareguard.rwx.json fix (git diff --output= / git show --output= / git log --output= all write; commit b87ca76). \"git diff\": \"r\" is KEPT here on purpose, unlike the shipped file — it is the one deliberately-risky row this POC needs to still demonstrate [KNOWN LIMIT #5] (git diff --output=... allowed read-only). See rwx-poc.md's Known Limit #5 writeup.",
  "agents": {
    "researcher": "r--",
    "fixer": "rw-",
    "deployer": "rwx"
  },
  "tools": {
    "read": "r",
    "fetch": "r",
    "search": "r",
    "write": "w",
    "edit": "w",
    "github.create_pr": "w",
    "deploy": "x",
    "wireMoney": "x"
  },
  "bash": {
    "ls": "r",
    "cat": "r",
    "grep": "r",
    "git status": "r",
    "git diff": "r",
    "git add": "w",
    "git commit": "w",
    "npm test": "w",
    "npm run build": "w",
    "npm publish": "x",
    "git push": "x",
    "rm": "x"
  }
}
